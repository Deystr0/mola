# Provider secrets are read at runtime and are never written to optimizer.yaml.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
OLLAMA_API_KEY=

# Required when remote authentication is enabled (including Docker example).
OPTIMIZER_API_KEY=

# Optional: point every CLI command at a non-default configuration file.
OPTIMIZER_CONFIG=
